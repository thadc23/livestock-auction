import auction.CheckoffType
import auction.Species


class BootStrap {

	def init = {
		servletContext ->
//		CheckoffType perHead = new CheckoffType(name: 'per head');
//		perHead.save(flush: true);
//		CheckoffType perPound = new CheckoffType(name: 'per pound');
//		perPound.save(flush: true);
//		CheckoffType gmv = new CheckoffType(name: 'gross market value');
//		gmv.save(flush: true);
//		CheckoffType none = new CheckoffType(name: 'none');
//		none.save(flush: true);
//		new Species(name: 'SWINE', checkoffAmount: 1, checkoffType: gmv).save(flush: true);
//		new Species(name: 'STEER', checkoffAmount: 1, checkoffType: perHead).save(flush: true);
//		new Species(name: 'RABBIT', checkoffAmount: 0, checkoffType: none).save(flush: true);
//		new Species(name: 'POULTRY', checkoffAmount: 0, checkoffType: none).save(flush: true);
//		new Species(name: 'GOAT', checkoffAmount: 0, checkoffType: none).save(flush: true);
//		new Species(name: 'SHEEP', checkoffAmount: 1, checkoffType: perPound).save(flush: true);
//		new Species(name: 'MISC', checkoffAmount: 0, checkoffType: none).save(flush: true);
	}
	def destroy = {
	}
}